CREATE DATABASE dbsubnet; 
USE dbsubnet; 
CREATE TABLE dbsubnet_table (NETWORK_ID VARCHAR(18) NOT NULL, BROADCAST_ID VARCHAR(18) NOT NULL, NUMBER_OF_HOSTS INT NOT NULL, CLASS VARCHAR(7) NOT NULL, PUBLIC_or_PRIVATE VARCHAR(10) NOT NULL);
SHOW TABLES;
DESCRIBE TABLES;
SELECT * FROM dbsubnet_table;
